<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Application Form</title>
    <link href="/output.css" rel="stylesheet">

</head>
<body class="bg-gray-200">
    <%- include('includes/header'); -%> 

    <div class="min-h-[84vh] flex items-center justify-center">
        <div class="bg-white p-8 rounded-lg shadow-lg max-w-md">
            <h1 class="text-xl font-bold mb-6">Card Application</h1>
            <% if (error) { %>
                <p class="text-red-500"><%= error %></p>
            <% } %>
            <% if (application) { %>
                    <label for="fullname" class="block text-sm font-medium text-gray-700">Full Name</label>
                    <input type="text" id="fullname" name="fullname" readonly value="<%=personalInformation.FirstName%> <%=personalInformation.LastName%>" class="mt-1 mb-4 p-2 w-full border rounded-md" required>
                    
                    <label for="dateofbirth" class="block text-sm font-medium text-gray-700">Date of Birth</label>
                    <input type="text" id="dateofbirth" name="dateofbirth" readonly value="<%=new Date(personalInformation.DOB).toISOString().slice(0, 10) %>" class="mt-1 mb-4 p-2 w-full border rounded-md" required>
    
                    <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
                    <input type="email" id="email" name="email" readonly value="<%=user.Email%>" class="mt-1 mb-4 p-2 w-full border rounded-md" required>
    
            <% } %>
            <% if (!application) { %>

           <form action="/submit-application" method="POST">
                <label for="fullname" class="block text-sm font-medium text-gray-700">Full Name</label>
                <input type="text" id="fullname" name="fullname" class="mt-1 mb-4 p-2 w-full border rounded-md" required>
                
                <label for="dateofbirth" class="block text-sm font-medium text-gray-700">Date of Birth</label>
                <input type="date" id="dateofbirth" name="dateofbirth" class="mt-1 mb-4 p-2 w-full border rounded-md" required>

                <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
                <input type="email" id="email" name="email" class="mt-1 mb-4 p-2 w-full border rounded-md" required>

                <button type="submit" class="w-full bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Submit Application</button>
            </form>
            <% } %>

        </div>
    </div>
    <%- include('includes/footer'); -%> 
 
</body>
</html>
